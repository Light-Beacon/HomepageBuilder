<local:MyCard Title="${title}" CanSwap="${canswap}" IsSwaped="${isswaped}" Style="{StaticResource Card}" >
    <StackPanel Style="{StaticResource ContentStack}">
        ${$ChildrenPresenter}
    </StackPanel>
</local:MyCard>